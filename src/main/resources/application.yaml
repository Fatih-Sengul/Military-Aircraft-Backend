spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/military_aircraft?currentSchema=military_aircraft_test
    username: postgres
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

server:
  port: 8080